class BottomNavBarItemModel {
  final String title;
  final String icon;
  final bool isSelected;

  BottomNavBarItemModel({
    required this.title,
    required this.icon,
    required this.isSelected,
  });
}